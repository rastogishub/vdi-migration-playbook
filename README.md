# vdi-migration-playbook
AWS-side infrastructure: VPC, WorkSpaces-ready subnets across two AZs, AppStream 2.0 fleet + stack (for the 100+ Citrix apps you migrated), Security Groups, NAT Gateway, and the WorkSpaces Directory with AD join.
